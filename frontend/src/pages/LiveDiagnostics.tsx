/**
 * Live Diagnostics Page
 * 
 * Main page for real-time diagnostics with ML-based anomaly detection
 */

import LiveDiagnosticsPanel from '../components/diagnostics/LiveDiagnosticsPanel';

export default function LiveDiagnostics() {
  return <LiveDiagnosticsPanel />;
}
